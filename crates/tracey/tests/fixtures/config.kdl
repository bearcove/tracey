// Tracey configuration for integration tests

spec {
    name "test"
    prefix "r"
    include "spec.md"

    impl {
        name "rust"
        include "src/**/*.rs"
        test-include "src/tests.rs"
    }
}
